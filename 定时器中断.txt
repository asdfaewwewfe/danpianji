定时器0：






定时器1：
串口专用定时器1。
对于串口中断，应该配置定时器1。这是特定的需求
串口中断的配置流程是：PCON=0x80;//操控SMOD=1使得串行口同行的波特率的效率翻倍
SCON=0x40;//控制工作方式，当工作方式为2，3时，SM2位为1，REN位为1(REN为允许接收状态，接收数据);
TMOD=>配置定时器1的内容
TL1、TH1配置定时器的初值=>由波特率决定
ET1=0；//禁止定时器1中断
TR1=1；//启动定时器1
